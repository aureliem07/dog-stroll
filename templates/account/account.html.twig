{% extends 'base.html.twig' %}

{% block title %}S'enregistrer{% endblock %}

{% block container %}

<div class="row gutters-sm m-5">
  <div class="col-lg-4">
    <div class="card shadow border-0">
    <div class="card-body">
        <div class="d-flex flex-column align-items-center text-center">
          <img src="https://bootdey.com/img/Content/avatar/avatar7.png" alt="profil" class="rounded-circle" width="150">
          <div class="mt-3">
            <h4>{{ app.user.lastName}} {{ app.user.firstName}}</h4>
            <p class="text-secondary mb-1">{{ app.user.nickname}}</p>
            <p class="text-muted font-size-sm">{{ app.user.town}}</p>
          </div>
      </div>
      </div>
    </div>
  </div>
  <div class="col-md-8">
    <div class="card shadow border-0">
      <div class="card-body">
        <div class="row">
          <div class="col-sm-3">
            <h6 class="mb-0">Prénom</h6>
          </div>
          <div class="col-sm-9 text-secondary">
            {{ app.user.lastName}}
          </div>
        </div>
        <hr>
        <div class="row">
          <div class="col-sm-3">
            <h6 class="mb-0">Nom</h6>
          </div>
          <div class="col-sm-9 text-secondary">
            {{ app.user.firstName}}
          </div>
        </div>
        <hr>
        <div class="row">
          <div class="col-sm-3">
            <h6 class="mb-0">Pseudo</h6>
          </div>
          <div class="col-sm-9 text-secondary">
            {{ app.user.nickname}}
          </div>
        </div>
        <hr>
        <div class="row">
          <div class="col-sm-3">
            <h6 class="mb-0">Mail</h6>
          </div>
          <div class="col-sm-9 text-secondary">
            {{ app.user.email}}
          </div>
        </div>
        <hr>
        <div class="row">
          <div class="col-sm-3">
            <h6 class="mb-0">Ville</h6>
          </div>
          <div class="col-sm-9 text-secondary">
            {{ app.user.town}}
          </div>
        </div>
        <hr>
        <div class="row">
          <div class="col-sm-3">
            <h6 class="mb-0">Bio</h6>
          </div>
          <div class="col-sm-9 text-secondary">
            {{ app.user.bio}}
          </div>
        </div>
        <hr>        
        {# <div class="row">
          <div class="col-sm-12">
            <a class="btn btn-warning" href="#">Modifier</a>
          </div>
        </div> #}
      </div>
    </div>
  </div>
</div>
{% endblock %}